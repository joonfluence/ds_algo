// 5(개), 8(덧셈횟수), 3(최대 횟수)
// 2, 4, 5, 4, 6

const greedy = (array) => {
  const result = 0;
  const max = 0;
  const secondMax = 0;

  // 1. 최댓값을 구한다.

  // 2. 최댓값을 M만큼 더한다.

  // 3. 두번째로 큰 값(혹은 같을 수도 있음)을 한 번 더한다.

  // 4. 다시 최댓값을 M만큼 더한다.

  // 예외 : 다른 인덱스에 같은 숫자가 존재할 경우를 고려할 필요가 있음.
};

console.log(`file name [02.js], line -> 8 >>>>> `, greedy([2, 4, 5, 4, 6]));
