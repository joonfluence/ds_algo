// The global variable
let fixedValue: number = 4;

function incrementer(): number {
  // Only change code below this line
  const number = fixedValue + 1;
  return number;
  // Only change code above this line
}
