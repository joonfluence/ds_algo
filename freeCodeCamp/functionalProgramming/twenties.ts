function checkPositiveWithEvery(arr: number[]) {
  // Only change code below this line
  return arr.every((item) => item > 0);
  // Only change code above this line
}

checkPositiveWithEvery([1, 2, 3, -4, 5]);
